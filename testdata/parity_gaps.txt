{"dialect":"hive","category":"parse: Expecting )","sql":"SELECT ROW() OVER (DISTRIBUTE BY x SORT BY y)"}
{"dialect":"hive","category":"parse: Invalid expression / Unexpected token","sql":"INSERT OVERWRITE DIRECTORY 'x' ROW FORMAT DELIMITED FIELDS TERMINATED BY '\u0001' COLLECTION ITEMS TERMINATED BY ',' MAP KEYS TERMINATED BY ':' LINES TERMINATED BY '' STORED AS TEXTFILE SELECT * FROM `a`.`b`"}
{"dialect":"hive","category":"parse: Invalid expression / Unexpected token","sql":"SELECT * FROM my_table TIMESTAMP AS OF DATE_ADD(CURRENT_DATE, -1)"}
{"dialect":"hive","category":"parse: Invalid expression / Unexpected token","sql":"SELECT * FROM my_table VERSION AS OF DATE_ADD(CURRENT_DATE, -1)"}
{"dialect":"hive","category":"parse: Invalid expression / Unexpected token","sql":"SELECT * FROM x TABLESAMPLE (1 PERCENT) AS foo"}
{"dialect":"hive","category":"parse: Invalid expression / Unexpected token","sql":"SELECT * FROM x.z TABLESAMPLE(10 PERCENT) y"}
{"dialect":"hive","category":"parse: Required keyword","sql":"SELECT ${hiveconf:some_var}"}
{"dialect":"presto","category":"gen: canonicalization","sql":"DATE_FORMAT(x, '%Y-%m-%d %H:%i:%S')"}
{"dialect":"presto","category":"gen: canonicalization","sql":"DATE_PARSE(x, '%Y-%m-%d %H:%i:%S')"}
{"dialect":"presto","category":"gen: canonicalization","sql":"JSON '\"foo\"'"}
{"dialect":"presto","category":"gen: canonicalization","sql":"JSON_FORMAT(JSON '\"x\"')"}
{"dialect":"presto","category":"gen: canonicalization","sql":"REGEXP_REPLACE('abcd', '[ab]')"}
{"dialect":"presto","category":"gen: canonicalization","sql":"TO_CHAR(ts, 'dd')"}
{"dialect":"presto","category":"gen: canonicalization","sql":"TO_CHAR(ts, 'hh')"}
{"dialect":"presto","category":"gen: canonicalization","sql":"TO_CHAR(ts, 'hh24')"}
{"dialect":"presto","category":"gen: canonicalization","sql":"TO_CHAR(ts, 'mi')"}
{"dialect":"presto","category":"gen: canonicalization","sql":"TO_CHAR(ts, 'mm')"}
{"dialect":"presto","category":"gen: canonicalization","sql":"TO_CHAR(ts, 'ss')"}
{"dialect":"presto","category":"gen: canonicalization","sql":"TO_CHAR(ts, 'yy')"}
{"dialect":"presto","category":"gen: canonicalization","sql":"TO_CHAR(ts, 'yyyy')"}
{"dialect":"presto","category":"gen: canonicalization","sql":"UNHEX(x)"}
{"dialect":"presto","category":"gen: cast/type","sql":"SELECT CAST(JSON '[1,23,456]' AS ARRAY(INTEGER))"}
{"dialect":"presto","category":"gen: cast/type","sql":"SELECT CAST(JSON '{\"k1\":1,\"k2\":23,\"k3\":456}' AS MAP(VARCHAR, INTEGER))"}
{"dialect":"presto","category":"gen: ddl/properties","sql":"CREATE TABLE test STORED AS 'PARQUET' AS SELECT 1"}
{"dialect":"presto","category":"parse: Invalid expression / Unexpected token","sql":"SELECT\n  *\nFROM orders\nMATCH_RECOGNIZE (\n  PARTITION BY custkey\n  ORDER BY\n    orderdate\n  MEASURES\n    A.totalprice AS starting_price,\n    LAST(B.totalprice) AS bottom_price,\n    LAST(C.totalprice) AS top_price\n  ONE ROW PER MATCH\n  AFTER MATCH SKIP PAST LAST ROW\n  PATTERN (A B+ C+ D+)\n  DEFINE\n    B AS totalprice < PREV(totalprice),\n    C AS totalprice > PREV(totalprice) AND totalprice <= A.totalprice,\n    D AS totalprice > PREV(totalprice),\n    E AS MAX(foo) >= NEXT(bar)\n)"}
{"dialect":"presto","category":"parse: Invalid expression / Unexpected token","sql":"SELECT * FROM example.testdb.customer_orders FOR TIMESTAMP AS OF CAST('2022-03-23 09:59:29.803 Europe/Vienna' AS TIMESTAMP)"}
{"dialect":"presto","category":"parse: Invalid expression / Unexpected token","sql":"SELECT * FROM example.testdb.customer_orders FOR VERSION AS OF 8954597067493422955"}
{"dialect":"presto","category":"parse: Invalid expression / Unexpected token","sql":"SELECT * FROM foo FOR TIMESTAMP AS OF CAST('2020-01-01 00:00:00' AS TIMESTAMP) AS bar"}
{"dialect":"presto","category":"parse: Invalid expression / Unexpected token","sql":"U&'Hello winter #2603 !' UESCAPE '#'"}
{"dialect":"presto","category":"parse: Invalid expression / Unexpected token","sql":"u&'Hello winter #2603 !' UESCAPE '#'"}
{"dialect":"trino","category":"parse: Expecting","sql":"WITH FUNCTION count_evens(n BIGINT) RETURNS BIGINT BEGIN DECLARE total BIGINT DEFAULT 0; DECLARE i BIGINT DEFAULT 0; WHILE i < n DO SET total = total + 1; IF i = 0 THEN SET total = total + 1; END IF; SET i = i + 1; END WHILE; RETURN total; END SELECT COUNT_EVENS(3)"}
{"dialect":"trino","category":"parse: Expecting","sql":"WITH FUNCTION custom_sqrt(a INTEGER) RETURNS DOUBLE COMMENT 'Custom sqrt function' RETURNS NULL ON NULL INPUT NOT DETERMINISTIC LANGUAGE SQL SECURITY DEFINER WITH (weight=42, cost='low') RETURN a SELECT CUSTOM_SQRT(4)"}
{"dialect":"trino","category":"parse: Expecting","sql":"WITH FUNCTION declare() RETURNS INTEGER RETURN 1 SELECT DECLARE()"}
{"dialect":"trino","category":"parse: Expecting","sql":"WITH FUNCTION doubled(x INTEGER) RETURNS INTEGER BEGIN RETURN x * 2; END WITH t AS (SELECT 3 AS v) SELECT DOUBLED(v) FROM t"}
{"dialect":"trino","category":"parse: Expecting","sql":"WITH FUNCTION doubled(x INTEGER) RETURNS INTEGER RETURN x * 2 WITH t AS (SELECT 3 AS v) SELECT DOUBLED(v) FROM t"}
{"dialect":"trino","category":"parse: Expecting","sql":"WITH FUNCTION f() RETURNS INTEGER BEGIN DECLARE first_name, last_name, middle_name VARCHAR(25); RETURN 1; END SELECT F()"}
{"dialect":"trino","category":"parse: Expecting","sql":"WITH FUNCTION f() RETURNS INTEGER BEGIN DECLARE x INTEGER DEFAULT 1; BEGIN SET x = x + 1; END; RETURN x; END SELECT F()"}
{"dialect":"trino","category":"parse: Expecting","sql":"WITH FUNCTION f() RETURNS INTEGER CALLED ON NULL INPUT RETURN 1 SELECT F()"}
{"dialect":"trino","category":"parse: Expecting","sql":"WITH FUNCTION f() RETURNS INTEGER COMMENT 'hi' RETURN 1 SELECT F()"}
{"dialect":"trino","category":"parse: Expecting","sql":"WITH FUNCTION f() RETURNS INTEGER DETERMINISTIC RETURN 1 SELECT F()"}
{"dialect":"trino","category":"parse: Expecting","sql":"WITH FUNCTION f() RETURNS INTEGER LANGUAGE SQL NOT DETERMINISTIC BEGIN DECLARE x INTEGER DEFAULT 1; RETURN x; END SELECT F()"}
{"dialect":"trino","category":"parse: Expecting","sql":"WITH FUNCTION f() RETURNS INTEGER LANGUAGE SQL RETURN 1 SELECT F()"}
{"dialect":"trino","category":"parse: Expecting","sql":"WITH FUNCTION f() RETURNS INTEGER LANGUAGE SQL WITH (weight=42, cost='low') RETURN 1 SELECT F()"}
{"dialect":"trino","category":"parse: Expecting","sql":"WITH FUNCTION f() RETURNS INTEGER NOT DETERMINISTIC RETURN 1 SELECT F()"}
{"dialect":"trino","category":"parse: Expecting","sql":"WITH FUNCTION f() RETURNS INTEGER RETURNS NULL ON NULL INPUT RETURN 1 SELECT F()"}
{"dialect":"trino","category":"parse: Expecting","sql":"WITH FUNCTION f() RETURNS INTEGER SECURITY DEFINER RETURN 1 SELECT F()"}
{"dialect":"trino","category":"parse: Expecting","sql":"WITH FUNCTION f() RETURNS INTEGER SECURITY INVOKER RETURN 1 SELECT F()"}
{"dialect":"trino","category":"parse: Expecting","sql":"WITH FUNCTION f() RETURNS INTEGER WITH (weight=42) RETURN 1 SELECT F()"}
{"dialect":"trino","category":"parse: Expecting","sql":"WITH FUNCTION f(a INTEGER) RETURNS INTEGER BEGIN DECLARE result INTEGER DEFAULT 0; IF a > 0 THEN RETURN CASE WHEN a > 10 THEN 1 ELSE 2 END; ELSE SET result = -1; END IF; RETURN result; END SELECT F(1)"}
{"dialect":"trino","category":"parse: Expecting","sql":"WITH FUNCTION f(a INTEGER) RETURNS VARCHAR BEGIN IF a = 0 THEN RETURN 'zero'; ELSEIF a = 1 THEN RETURN 'one'; END IF; RETURN 'other'; END SELECT F(1)"}
{"dialect":"trino","category":"parse: Expecting","sql":"WITH FUNCTION f(a INTEGER) RETURNS VARCHAR BEGIN IF a = 0 THEN RETURN 'zero'; END IF; RETURN 'other'; END SELECT F(1)"}
{"dialect":"trino","category":"parse: Expecting","sql":"WITH FUNCTION f(a INTEGER, b INTEGER) RETURNS VARCHAR BEGIN IF a = 0 THEN IF b = 0 THEN RETURN 'both zero'; ELSE RETURN 'a zero'; END IF; ELSE RETURN 'a nonzero'; END IF; RETURN 'unreachable'; END SELECT F(1, 2)"}
{"dialect":"trino","category":"parse: Expecting","sql":"WITH FUNCTION f(num INTEGER) RETURNS INTEGER RETURN num SELECT F(1)"}
{"dialect":"trino","category":"parse: Expecting","sql":"WITH FUNCTION f(num int)\n    RETURNS int\n    RETURN num\nSELECT f(1)"}
{"dialect":"trino","category":"parse: Expecting","sql":"WITH FUNCTION f(x INTEGER) RETURNS INTEGER RETURN x WITH RECURSIVE t(n) AS (SELECT 1 AS n) SELECT F(n) FROM t"}
{"dialect":"trino","category":"parse: Expecting","sql":"WITH FUNCTION hello(name VARCHAR) RETURNS VARCHAR RETURN FORMAT('Hello %s!', name), FUNCTION bye() RETURNS VARCHAR RETURN 'Bye!' SELECT HELLO('Finn') || BYE()"}
{"dialect":"trino","category":"parse: Expecting","sql":"WITH FUNCTION iter_count() RETURNS BIGINT BEGIN DECLARE a BIGINT DEFAULT 0; DECLARE b BIGINT DEFAULT 0; top: REPEAT SET a = a + 1; IF a <= 3 THEN ITERATE top; END IF; SET b = b + 1; UNTIL a >= 10 END REPEAT; RETURN b; END SELECT ITER_COUNT()"}
{"dialect":"trino","category":"parse: Expecting","sql":"WITH FUNCTION label_test(n BIGINT) RETURNS BIGINT BEGIN DECLARE i BIGINT DEFAULT 0; iterate: LOOP IF i >= n THEN LEAVE iterate; END IF; SET i = i + 1; END LOOP; RETURN i; END SELECT LABEL_TEST(5)"}
{"dialect":"trino","category":"parse: Expecting","sql":"WITH FUNCTION label_test2(n BIGINT) RETURNS BIGINT BEGIN DECLARE i BIGINT DEFAULT 0; leave: LOOP IF i >= n THEN LEAVE leave; END IF; SET i = i + 1; END LOOP; RETURN i; END SELECT LABEL_TEST2(5)"}
{"dialect":"trino","category":"parse: Expecting","sql":"WITH FUNCTION label_test3(n BIGINT) RETURNS BIGINT BEGIN DECLARE i BIGINT DEFAULT 0; set: LOOP IF i >= n THEN LEAVE set; END IF; SET i = i + 1; END LOOP; RETURN i; END SELECT LABEL_TEST3(5)"}
{"dialect":"trino","category":"parse: Expecting","sql":"WITH FUNCTION last_stmt(a INTEGER) RETURNS VARCHAR BEGIN CASE a WHEN 0 THEN RETURN 'zero'; ELSE RETURN 'other'; END CASE; END SELECT LAST_STMT(1)"}
{"dialect":"trino","category":"parse: Expecting","sql":"WITH FUNCTION last_stmt(n BIGINT) RETURNS BIGINT BEGIN DECLARE i BIGINT DEFAULT 0; top: LOOP IF i >= n THEN LEAVE top; END IF; SET i = i + 1; END LOOP; END SELECT LAST_STMT(5)"}
{"dialect":"trino","category":"parse: Expecting","sql":"WITH FUNCTION last_stmt(n BIGINT) RETURNS BIGINT BEGIN DECLARE r BIGINT DEFAULT 1; WHILE r < n DO SET r = r + 1; END WHILE; END SELECT LAST_STMT(5)"}
{"dialect":"trino","category":"parse: Expecting","sql":"WITH FUNCTION meaning_of_life() RETURNS INTEGER BEGIN DECLARE a INTEGER DEFAULT 6; DECLARE b INTEGER DEFAULT 7; RETURN a * b; END SELECT MEANING_OF_LIFE()"}
{"dialect":"trino","category":"parse: Expecting","sql":"WITH FUNCTION mix(a INTEGER) RETURNS INTEGER BEGIN DECLARE result INTEGER DEFAULT 0; CASE WHEN a > 0 THEN IF a > 10 THEN SET result = 1; ELSE SET result = 2; END IF; ELSE SET result = CASE WHEN a < -10 THEN -1 ELSE -2 END; END CASE; RETURN result; END SELECT MIX(1)"}
{"dialect":"trino","category":"parse: Expecting","sql":"WITH FUNCTION nested_case(a BIGINT, b BIGINT) RETURNS VARCHAR BEGIN DECLARE result VARCHAR; CASE WHEN a = 0 THEN CASE b WHEN 0 THEN SET result = 'a0b0'; ELSE SET result = 'a0bN'; END CASE; ELSE SET result = 'aN'; END CASE; RETURN result; END SELECT NESTED_CASE(0, 0)"}
{"dialect":"trino","category":"parse: Expecting","sql":"WITH FUNCTION nested_test(n BIGINT) RETURNS BIGINT BEGIN DECLARE total BIGINT DEFAULT 0; DECLARE i BIGINT DEFAULT 0; DECLARE j BIGINT DEFAULT 0; outer_loop: WHILE i < n DO SET j = 0; inner_loop: LOOP IF j >= 3 THEN LEAVE inner_loop; END IF; SET total = total + 1; SET j = j + 1; END LOOP; SET i = i + 1; END WHILE; RETURN total; END SELECT NESTED_TEST(2)"}
{"dialect":"trino","category":"parse: Expecting","sql":"WITH FUNCTION no_else(a BIGINT) RETURNS VARCHAR BEGIN CASE a WHEN 0 THEN RETURN 'zero'; END CASE; RETURN 'fallthrough'; END SELECT NO_ELSE(0)"}
{"dialect":"trino","category":"parse: Expecting","sql":"WITH FUNCTION no_operand_no_else(a INTEGER) RETURNS VARCHAR BEGIN CASE WHEN a = 0 THEN RETURN 'zero'; END CASE; RETURN 'other'; END SELECT NO_OPERAND_NO_ELSE(1)"}
{"dialect":"trino","category":"parse: Expecting","sql":"WITH FUNCTION npow(n BIGINT) RETURNS BIGINT BEGIN DECLARE r BIGINT DEFAULT 1; DECLARE p BIGINT DEFAULT n; WHILE p > 1 DO SET r = r * n; SET p = p - 1; END WHILE; RETURN r; END SELECT NPOW(5)"}
{"dialect":"trino","category":"parse: Expecting","sql":"WITH FUNCTION npow_labeled(n BIGINT) RETURNS BIGINT BEGIN DECLARE r BIGINT DEFAULT 1; DECLARE p BIGINT DEFAULT n; abc: WHILE p > 1 DO SET r = r * n; SET p = p - 1; END WHILE; RETURN r; END SELECT NPOW_LABELED(5)"}
{"dialect":"trino","category":"parse: Expecting","sql":"WITH FUNCTION one() RETURNS INTEGER BEGIN DECLARE counter INTEGER DEFAULT 1; SET counter = 0; SET counter = counter + 2; SET counter = counter / counter; RETURN counter; END SELECT ONE()"}
{"dialect":"trino","category":"parse: Expecting","sql":"WITH FUNCTION searched_case(a BIGINT) RETURNS VARCHAR BEGIN CASE WHEN a = 0 THEN RETURN 'zero'; WHEN a = 1 THEN RETURN 'one'; ELSE RETURN 'other'; END CASE; RETURN NULL; END SELECT SEARCHED_CASE(0)"}
{"dialect":"trino","category":"parse: Expecting","sql":"WITH FUNCTION simple_case(a BIGINT) RETURNS VARCHAR BEGIN CASE a WHEN 0 THEN RETURN 'zero'; WHEN 1 THEN RETURN 'one'; ELSE RETURN 'more than one or negative'; END CASE; RETURN NULL; END SELECT SIMPLE_CASE(0)"}
{"dialect":"trino","category":"parse: Expecting","sql":"WITH FUNCTION simple_if(a BIGINT) RETURNS VARCHAR BEGIN IF a = 0 THEN RETURN 'zero'; ELSEIF a = 1 THEN RETURN 'one'; ELSE RETURN 'more than one or negative'; END IF; END SELECT SIMPLE_IF(3)"}
{"dialect":"trino","category":"parse: Expecting","sql":"WITH FUNCTION test_repeat(a BIGINT) RETURNS BIGINT BEGIN REPEAT SET a = a + 1; UNTIL a >= 10 END REPEAT; RETURN a; END SELECT TEST_REPEAT(5)"}
{"dialect":"trino","category":"parse: Expecting","sql":"WITH FUNCTION to_one_hundred(start_value BIGINT, step BIGINT) RETURNS BIGINT BEGIN DECLARE count BIGINT DEFAULT 0; DECLARE current BIGINT DEFAULT 0; SET current = start_value; abc: LOOP IF current >= 100 THEN LEAVE abc; END IF; SET count = count + 1; SET current = current + step; END LOOP; RETURN count; END SELECT TO_ONE_HUNDRED(0, 10)"}
{"dialect":"trino","category":"parse: Expecting","sql":"WITH FUNCTION while_case(n BIGINT) RETURNS BIGINT BEGIN DECLARE i BIGINT DEFAULT 0; WHILE i < n DO CASE WHEN i = 0 THEN SET i = 1; ELSE SET i = i + 1; END CASE; END WHILE; RETURN i; END SELECT WHILE_CASE(3)"}
{"dialect":"trino","category":"parse: Invalid expression / Unexpected token","sql":"SELECT * FROM tbl MATCH_RECOGNIZE (PARTITION BY id ORDER BY col MEASURES FIRST(col, 2) AS col1, LAST(col, 2) AS col2 PATTERN (B* A) DEFINE A AS col = 1)"}
